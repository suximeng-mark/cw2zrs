[project]
name = "com-classwidgets-duty-student"
version = "1.0.0"
description = "Class Widgets 2 值日生显示插件"
authors = [
    {name = ""},
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["class-widgets-sdk>=0.4.2"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
