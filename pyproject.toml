[project]
name = "com-studentondutyshow-com"
version = "1.1.0"
description = "Class Widgets 2 值日生插件，支持自定义分组、按周/每天/工作日轮换"
authors = [
    {name = "zrs"},
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["class-widgets-sdk>=0.4.2"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
